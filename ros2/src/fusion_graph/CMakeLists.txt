cmake_minimum_required(VERSION 3.8)
project(fusion_graph)

if(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
  add_compile_options(-Wall -Wextra -Wpedantic)
endif()

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

# ---------------------------------------------------------------------------
# Dependencies
# ---------------------------------------------------------------------------

find_package(ament_cmake        REQUIRED)
find_package(rclcpp             REQUIRED)
find_package(nav_msgs           REQUIRED)
find_package(geometry_msgs      REQUIRED)
find_package(sensor_msgs        REQUIRED)
find_package(std_msgs           REQUIRED)
find_package(std_srvs           REQUIRED)
find_package(diagnostic_msgs    REQUIRED)
find_package(visualization_msgs REQUIRED)
find_package(tf2                REQUIRED)
find_package(tf2_ros            REQUIRED)
find_package(tf2_geometry_msgs  REQUIRED)
find_package(tf2_eigen          REQUIRED)
find_package(mowgli_interfaces  REQUIRED)
find_package(Eigen3             REQUIRED)

# GTSAM ships its own CMake config; require >= 4.2 (Pose2 + ISAM2 stable
# API). Docker and CI retain the pinned 4.3a1 source install.
find_package(GTSAM CONFIG REQUIRED)

# LiDAR map anchor: Beluga particle filter (header-only MCL toolkit; the
# beluga_ros wrapper brings the Amcl engine and the OccupancyGrid adapter).
find_package(beluga_ros REQUIRED)

# ---------------------------------------------------------------------------
# Library: fusion_graph_core (factors + graph manager, header-light)
# ---------------------------------------------------------------------------

add_library(fusion_graph_core
  src/factors.cpp
  src/graph_manager.cpp
  src/graph_manager_node.cpp
  src/graph_manager_rebase.cpp
  src/graph_manager_persistence.cpp
  src/lidar_submap_store.cpp
)

target_include_directories(fusion_graph_core PUBLIC
  $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
  $<INSTALL_INTERFACE:include>
  ${EIGEN3_INCLUDE_DIRS}
)

# GTSAM is exported via its imported target `gtsam`. Linking publicly so
# downstream targets (the executable + tests) inherit the include dirs.
target_link_libraries(fusion_graph_core PUBLIC
  gtsam
  Eigen3::Eigen
)

target_link_libraries(fusion_graph_core PUBLIC
  ${rclcpp_TARGETS}
  ${nav_msgs_TARGETS}
  ${geometry_msgs_TARGETS}
  ${sensor_msgs_TARGETS}
  ${std_msgs_TARGETS}
  ${std_srvs_TARGETS}
  ${diagnostic_msgs_TARGETS}
  ${visualization_msgs_TARGETS}
  ${tf2_TARGETS}
  ${tf2_ros_TARGETS}
  ${tf2_geometry_msgs_TARGETS}
  ${tf2_eigen_TARGETS}
  ${mowgli_interfaces_TARGETS}
)

# ---------------------------------------------------------------------------
# Executable: fusion_graph_node
# ---------------------------------------------------------------------------

add_executable(fusion_graph_node
  src/fusion_graph_node.cpp
  src/fusion_graph_node_setup_params.cpp
  src/fusion_graph_node_setup_comms.cpp
  src/fusion_graph_node_callbacks_a.cpp
  src/fusion_graph_node_callbacks_b.cpp
  src/fusion_graph_node_misc.cpp
  src/fusion_graph_node_timer.cpp
  src/fusion_graph_node_publish.cpp
  src/fusion_graph_node_lidar_anchor.cpp
)

target_link_libraries(fusion_graph_node
  fusion_graph_core
)
target_link_libraries(fusion_graph_node
  ${beluga_ros_TARGETS}
)

# ---------------------------------------------------------------------------
# Install
# ---------------------------------------------------------------------------

install(
  DIRECTORY include/
  DESTINATION include
)

install(
  TARGETS
    fusion_graph_core
    fusion_graph_node
  ARCHIVE DESTINATION lib
  LIBRARY DESTINATION lib
  RUNTIME DESTINATION lib/${PROJECT_NAME}
)

install(
  DIRECTORY config launch
  DESTINATION share/${PROJECT_NAME}
)

# ---------------------------------------------------------------------------
# Testing
# ---------------------------------------------------------------------------

if(BUILD_TESTING)
  find_package(ament_lint_auto      REQUIRED)
  # uncrustify is replaced by clang-format (see .clang-format) — both are
  # style linters and disagree on subtle rules.
  set(ament_cmake_uncrustify_FOUND TRUE)
  # ament_copyright requires the full GPL license text; the project uses
  # SPDX identifiers (`SPDX-License-Identifier: GPL-3.0-or-later`) which
  # ament doesn't recognise as a known license — skip the lint.
  set(ament_cmake_copyright_FOUND TRUE)
  # cpplint enforces Google C++ style; clang-format with the project's
  # .clang-format covers the same ground with our own rules.
  set(ament_cmake_cpplint_FOUND TRUE)
  # flake8 + pep257 are PEP 8 / docstring linters for Python; the launch
  # files in this package follow the project's relaxed Python style.
  set(ament_cmake_flake8_FOUND TRUE)
  set(ament_cmake_pep257_FOUND TRUE)
  ament_lint_auto_find_test_dependencies()

  find_package(ament_cmake_gtest REQUIRED)

  ament_add_gtest(test_factors
    test/test_factors.cpp
  )
  target_link_libraries(test_factors fusion_graph_core)

  ament_add_gtest(test_covariance_frame
    test/test_covariance_frame.cpp
  )
  target_link_libraries(test_covariance_frame fusion_graph_core)

  ament_add_gtest(test_persistence
    test/test_persistence.cpp
  )
  target_link_libraries(test_persistence fusion_graph_core)

  ament_add_gtest(test_stationary_yaw
    test/test_stationary_yaw.cpp
  )
  target_link_libraries(test_stationary_yaw fusion_graph_core)

  ament_add_gtest(test_slip_pivot
    test/test_slip_pivot.cpp
  )
  target_link_libraries(test_slip_pivot fusion_graph_core)

  ament_add_gtest(test_graph_window
    test/test_graph_window.cpp
  )
  target_link_libraries(test_graph_window fusion_graph_core)

  ament_add_gtest(test_adaptive_noise
    test/test_adaptive_noise.cpp
  )
  target_link_libraries(test_adaptive_noise fusion_graph_core)

  ament_add_gtest(test_wheel_sigma_scaling
    test/test_wheel_sigma_scaling.cpp
  )
  target_link_libraries(test_wheel_sigma_scaling fusion_graph_core)

  ament_add_gtest(test_pose_extrapolator
    test/test_pose_extrapolator.cpp
  )
  target_link_libraries(test_pose_extrapolator fusion_graph_core)

  ament_add_gtest(test_map_odom_slew
    test/test_map_odom_slew.cpp
  )
  target_link_libraries(test_map_odom_slew fusion_graph_core)

  ament_add_gtest(test_yaw_gates
    test/test_yaw_gates.cpp
  )
  target_link_libraries(test_yaw_gates fusion_graph_core)

  ament_add_gtest(test_clear_graph_gate
    test/test_clear_graph_gate.cpp
  )
  target_link_libraries(test_clear_graph_gate fusion_graph_core)

  ament_add_gtest(test_dr_slip_veto
    test/test_dr_slip_veto.cpp
  )
  target_link_libraries(test_dr_slip_veto fusion_graph_core)

  ament_add_gtest(test_rtk_wrongfix_gate
    test/test_rtk_wrongfix_gate.cpp
  )
  target_link_libraries(test_rtk_wrongfix_gate fusion_graph_core)

  ament_add_gtest(test_dock_gps_consistency
    test/test_dock_gps_consistency.cpp
  )
  target_link_libraries(test_dock_gps_consistency fusion_graph_core)

  ament_add_gtest(test_slip_window
    test/test_slip_window.cpp
  )
  target_link_libraries(test_slip_window fusion_graph_core)

  ament_add_gtest(test_lidar_occupancy_mapper
    test/test_lidar_occupancy_mapper.cpp
  )
  target_link_libraries(test_lidar_occupancy_mapper fusion_graph_core)

  ament_add_gtest(test_lidar_map_anchor_gate
    test/test_lidar_map_anchor_gate.cpp
  )
  target_link_libraries(test_lidar_map_anchor_gate fusion_graph_core)
  ament_add_gtest(test_lidar_anchor_validator
    test/test_lidar_anchor_validator.cpp
  )
  target_link_libraries(test_lidar_anchor_validator fusion_graph_core)
  ament_add_gtest(test_lidar_anchor_odom
    test/test_lidar_anchor_odom.cpp
  )
  target_link_libraries(test_lidar_anchor_odom fusion_graph_core)
  target_link_libraries(test_lidar_anchor_odom
    ${beluga_ros_TARGETS}
  )  # Sophus comes with beluga_ros
  ament_add_gtest(test_lidar_map_file
    test/test_lidar_map_file.cpp
  )
  target_link_libraries(test_lidar_map_file fusion_graph_core)
  ament_add_gtest(test_lidar_anchor_shadow_stats
    test/test_lidar_anchor_shadow_stats.cpp
  )
  target_link_libraries(test_lidar_anchor_shadow_stats fusion_graph_core)

  ament_add_gtest(test_lidar_submap_store test/test_lidar_submap_store.cpp)
  target_link_libraries(test_lidar_submap_store fusion_graph_core)
  ament_add_gtest(test_lidar_compute_gate test/test_lidar_compute_gate.cpp)
  target_link_libraries(test_lidar_compute_gate fusion_graph_core)

  ament_add_gtest(test_lidar_scan_history test/test_lidar_scan_history.cpp)
  target_link_libraries(test_lidar_scan_history fusion_graph_core)

  ament_add_gtest(test_cog_flip_recovery
    test/test_cog_flip_recovery.cpp
  )
  target_link_libraries(test_cog_flip_recovery fusion_graph_core)

  ament_add_gtest(test_gyro_bias
    test/test_gyro_bias.cpp
  )
  target_link_libraries(test_gyro_bias fusion_graph_core)

  ament_add_gtest(test_rigid_transform
    test/test_rigid_transform.cpp
  )
  target_link_libraries(test_rigid_transform fusion_graph_core)

  ament_add_gtest(test_lidar_map_xy_prior
    test/test_lidar_map_xy_prior.cpp
  )
  target_link_libraries(test_lidar_map_xy_prior fusion_graph_core)

  ament_add_gtest(test_gnss_timestamp
    test/test_gnss_timestamp.cpp
  )
  target_link_libraries(test_gnss_timestamp fusion_graph_core)

  ament_add_gtest(test_gnss_observation_freshness
    test/test_gnss_observation_freshness.cpp
  )
  target_link_libraries(test_gnss_observation_freshness fusion_graph_core)

  # Performance benchmarks. Long-running (~30 s on ARM); only meaningful
  # in Release builds. Output is grep-friendly for tracking regressions.
  ament_add_gtest(test_perf
    test/test_perf.cpp
    TIMEOUT 300
  )
  target_link_libraries(test_perf fusion_graph_core)
endif()

# ---------------------------------------------------------------------------
# Package export
# ---------------------------------------------------------------------------

ament_export_include_directories(include)
ament_export_libraries(fusion_graph_core)
ament_export_dependencies(
  rclcpp
  nav_msgs
  geometry_msgs
  sensor_msgs
  std_msgs
  std_srvs
  diagnostic_msgs
  visualization_msgs
  tf2
  tf2_ros
  tf2_geometry_msgs
  tf2_eigen
  mowgli_interfaces
  Eigen3
)

ament_package()
