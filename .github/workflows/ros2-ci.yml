name: ROS2 — CI

on:
  push:
    branches: [main, dev, 'feat/**', 'fix/**', 'refactor/**', 'chore/**', 'perf/**']
    paths:
      - 'ros2/**'
      - 'tools/motor/**'
      - 'install/config/mowgli/**'
      - '.github/workflows/ros2-ci.yml'
  # NOTE: deliberately NO `paths:` filter here, and `dev` is included alongside
  # `main`. Two bugs used to hide in this block:
  #   1. `branches: [main]` meant a ROS2 change opened against `dev` — where all
  #      feature work lands — was never built, tested, cppcheck'd or
  #      format-checked. Branch-name luck (`fix/**` etc. matching the `push`
  #      trigger above) was the only thing that ran CI on those PRs; a
  #      `codex/fix/...` or `feature/...` branch got nothing.
  #   2. `Build & Test (ROS2 kilted)` is a REQUIRED status check on `dev`. A
  #      workflow skipped by a `paths:` filter reports NOTHING, so the required
  #      check sat pending forever and every GUI-only PR was permanently
  #      BLOCKED. A job skipped by an `if:` condition, in contrast, reports as
  #      skipped and satisfies branch protection — hence the `changes` job
  #      below, which moves the path filtering from the trigger into a job gate.
  pull_request:
    branches: [main, dev]

concurrency:
  group: ros2-ci-${{ github.ref }}
  cancel-in-progress: true

env:
  ROS_DISTRO: kilted

jobs:
  # ─── Change Detection ────────────────────────────────────────────────────────
  # The `pull_request` trigger has no `paths:` filter (see the comment on it) so
  # that the REQUIRED `Build & Test (ROS2 kilted)` check always reports. This job
  # reinstates the same filtering one level down, as a job gate: on a PR that
  # touches no ROS2 path, the four jobs below are skipped by `if:` — and a
  # job skipped that way is reported to branch protection as satisfied, whereas a
  # workflow skipped by a trigger-level `paths:` filter reports nothing at all.
  # On `push` (already path-filtered at the trigger) this always answers true.
  changes:
    name: Detect ROS2 changes
    runs-on: ubuntu-24.04
    outputs:
      ros2: ${{ steps.filter.outputs.ros2 }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Detect ROS2-relevant paths
        id: filter
        env:
          # Via env, never inlined into the script — a branch name is attacker
          # controlled on a fork PR.
          BASE_REF: ${{ github.base_ref }}
          EVENT_NAME: ${{ github.event_name }}
        run: |
          if [ "$EVENT_NAME" != "pull_request" ]; then
            echo "Not a pull_request — trigger-level paths: filter already applied."
            echo "ros2=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git fetch --no-tags --depth=200 origin "$BASE_REF" \
            || git fetch --no-tags origin "$BASE_REF" || true
          BASE=$(git merge-base FETCH_HEAD HEAD 2>/dev/null || true)
          if [ -z "$BASE" ]; then
            echo "No merge-base with $BASE_REF — running the full suite to fail safe."
            echo "ros2=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          CHANGED=$(git diff --name-only "$BASE" HEAD -- \
            'ros2/**' 'tools/motor/**' 'install/config/mowgli/**' \
            '.github/workflows/ros2-ci.yml')
          if [ -n "$CHANGED" ]; then
            echo "ROS2-relevant changes:"; echo "$CHANGED"
            echo "ros2=true" >> "$GITHUB_OUTPUT"
          else
            echo "No ROS2-relevant changes — downstream jobs will be skipped."
            echo "ros2=false" >> "$GITHUB_OUTPUT"
          fi

  # ─── Config Drift Gate ───────────────────────────────────────────────────────
  # Guards Architecture Invariant 15: the installed mowgli_robot.yaml is SPARSE
  # over the in-package template. check_config_drift.py fails if a STRUCTURAL
  # field (chassis geometry, wheel kinematics, sensor mounts) is defined in both
  # files with divergent values, or added to the installed file with no template
  # default — either of which silently gives Nav2/URDF/coverage the wrong
  # chassis. It also fails (issue #381) if the committed sparse file pads a key
  # with a value EQUAL to its template default — a no-op at merge time that
  # silently pins robots to a stale value when the template default is later
  # changed. No ROS2 setup needed; runs in seconds as a hard gate.
  config-drift:
    name: Config Drift (mowgli_robot.yaml)
    runs-on: ubuntu-24.04
    needs: changes
    if: needs.changes.outputs.ros2 == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Check mowgli_robot.yaml drift + padded defaults
        run: |
          python3 -m pip install --quiet pyyaml
          python3 ros2/scripts/check_config_drift.py

      - name: Unit-test check_config_drift.py
        run: |
          python3 -m pip install --quiet pytest
          python3 -m pytest -q ros2/scripts/test_check_config_drift.py

  # ─── Build & Test ────────────────────────────────────────────────────────────
  # NO `strategy.matrix` here, deliberately, and the name is a literal.
  # `Build & Test (ROS2 kilted)` is the required status check on `dev`, and a
  # single-entry matrix broke that name in BOTH directions:
  #   - skipped by `if:`  -> GitHub cannot evaluate the matrix, so it published
  #     the raw template `Build & Test (ROS2 ${{ env.ROS_DISTRO }})`;
  #   - actually running  -> GitHub appends the matrix context, publishing
  #     `Build & Test (ROS2 kilted) (kilted, 24.04)`.
  # Neither equals the protected name, so the required check never reported.
  # The matrix only ever had one entry and only `ros_distro` was referenced, so
  # it is collapsed onto the existing global `env.ROS_DISTRO`. If a second distro
  # is ever needed, reinstate the matrix AND update the branch-protection rule to
  # the matrix-suffixed names.
  build-and-test:
    name: Build & Test (ROS2 kilted)
    runs-on: ubuntu-24.04
    needs: changes
    if: needs.changes.outputs.ros2 == 'true'

    defaults:
      run:
        working-directory: ros2

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          # opennav_coverage is a git submodule providing opennav_coverage_msgs
          # (a source package in the workspace). Without it checked out, rosdep
          # can't see it as a source dep and `rosdep install --ignore-src`
          # fails with "Cannot locate rosdep definition for
          # [opennav_coverage_msgs]", blocking the build+test. (ros2-docker.yml
          # already checks out submodules; this CI didn't — so tests never ran.)
          submodules: recursive

      - name: Link sidecar ROS2 tools package into workspace
        working-directory: ros2
        run: |
          mkdir -p src
          rm -rf src/mowgli_tools
          ln -s ../../tools/motor src/mowgli_tools
          ls -la src
          ls -la src/mowgli_tools
          test -f src/mowgli_tools/package.xml

      - name: Preinstall ROS apt source package
        working-directory: .
        run: |
          # setup-ros currently synthesizes the GitHub release asset filename
          # for ros2-apt-source. Preinstall the package from the release
          # metadata instead so CI follows the published asset URL directly.
          bash .github/scripts/install-ros-apt-source.sh ros2-apt-source

      - name: Setup ROS2 ${{ env.ROS_DISTRO }}
        uses: ros-tooling/setup-ros@v0.7
        with:
          required-ros-distributions: ${{ env.ROS_DISTRO }}

      - name: Validate mowgli_tools package discovery
        run: |
          source /opt/ros/${{ env.ROS_DISTRO }}/setup.bash
          colcon list --base-paths src | tee /tmp/colcon-list.txt
          grep -E '^mowgli_tools\s' /tmp/colcon-list.txt

      # GTSAM 4.3a1 is required by fusion_graph (find_package(GTSAM CONFIG
      # REQUIRED) in its CMakeLists). The Ubuntu apt package ships 4.2 with
      # a broken GTSAMConfig.cmake AND the legacy boost::optional<Matrix&>
      # custom-factor API, so the Docker image build (ros2/Dockerfile stage 0)
      # builds 4.3a1 from source and lands it under /opt/gtsam. We mirror the
      # same recipe here, cached so subsequent runs are instant.
      - name: Cache GTSAM build
        id: cache-gtsam
        uses: actions/cache@v4
        with:
          path: /opt/gtsam
          key: gtsam-4.3a1-${{ runner.os }}-noble

      - name: Build GTSAM 4.3a1 from source
        working-directory: .
        if: steps.cache-gtsam.outputs.cache-hit != 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential cmake git \
            libboost-all-dev libeigen3-dev libmetis-dev
          cd /tmp
          git clone --depth 1 -b 4.3a1 https://github.com/borglab/gtsam.git
          cd gtsam && mkdir build && cd build
          cmake .. \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX=/opt/gtsam \
            -DGTSAM_BUILD_TESTS=OFF \
            -DGTSAM_BUILD_EXAMPLES_ALWAYS=OFF \
            -DGTSAM_BUILD_PYTHON=OFF \
            -DGTSAM_BUILD_UNSTABLE=OFF \
            -DGTSAM_USE_SYSTEM_EIGEN=ON \
            -DGTSAM_USE_SYSTEM_METIS=ON \
            -DGTSAM_WITH_TBB=OFF \
            -DGTSAM_WITH_EIGEN_MKL=OFF \
            -DGTSAM_INSTALL_MATLAB_TOOLBOX=OFF \
            -DBUILD_SHARED_LIBS=ON
          sudo make -j$(nproc) install
          # The ldconfig + CMAKE_PREFIX_PATH setup is done in subsequent
          # steps so that cached restores also see the library.

      - name: Register GTSAM in linker + CMake search paths
        run: |
          echo "/opt/gtsam/lib" | sudo tee /etc/ld.so.conf.d/gtsam.conf
          sudo ldconfig
          echo "CMAKE_PREFIX_PATH=/opt/gtsam:$CMAKE_PREFIX_PATH" >> $GITHUB_ENV
          echo "GTSAM_DIR=/opt/gtsam/lib/cmake/GTSAM" >> $GITHUB_ENV
          # Pre-install runtime apt deps that GTSAM links against (boost,
          # eigen, metis), in case the cache restore skipped the apt step.
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libboost-all-dev libeigen3-dev libmetis-dev

      # Fields2Cover v3 — mowgli_coverage pins
      #   find_package(Fields2Cover 3.0.0 REQUIRED PATHS /opt/fields2cover-300)
      # v3 is UNRELEASED, so we PIN a commit SHA (matches the Dockerfile
      # fields2cover-v3-builder stage). No debian on Noble, so the Docker image
      # builds it from source; mirror that here (cached) or the workspace build
      # fails at mowgli_coverage. The cache key includes the SHA so it
      # invalidates when the pin moves.
      - name: Cache Fields2Cover build
        id: cache-f2c
        uses: actions/cache@v4
        with:
          path: /opt/fields2cover-300
          key: f2c-3.0.0-884d895-${{ runner.os }}-noble

      - name: Build Fields2Cover v3 from source
        working-directory: .
        if: steps.cache-f2c.outputs.cache-hit != 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential cmake git pkg-config \
            libeigen3-dev libgdal-dev libgeos++-dev libgeos-dev libgsl-dev \
            libtinyxml2-dev libpython3-dev python3-dev libtbb-dev nlohmann-json3-dev \
            ros-${{ env.ROS_DISTRO }}-ortools-vendor
          source /opt/ros/${{ env.ROS_DISTRO }}/setup.bash
          cd /tmp
          # Pinned to v3.0 @ 884d895 — keep in sync with ros2/Dockerfile. Patch the
          # upstream missing <iomanip> include (std::setw/setfill in visualizer.cpp;
          # modern GCC no longer pulls it in transitively).
          git clone https://github.com/Fields2Cover/Fields2Cover.git f2c3
          cd f2c3 && git checkout 884d895b59192882476e986ba44ea9143a06a6a9
          sed -i '1i #include <iomanip>' src/fields2cover/utils/visualizer.cpp
          mkdir build && cd build
          cmake .. \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX=/opt/fields2cover-300 \
            -DBUILD_TESTS=OFF \
            -DBUILD_PYTHON=OFF \
            -DBUILD_TUTORIALS=OFF \
            -DBUILD_DOC=OFF \
            -DALLOW_PARALLELIZATION=ON \
            -DBUILD_SHARED_LIBS=ON
          sudo make -j$(nproc) install

      - name: Register Fields2Cover in linker + CMake search paths
        run: |
          # Runtime/link deps mowgli_coverage needs against F2C (the build apt
          # deps above cover these, but a cache-restore skips that step).
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libgdal-dev libgeos-dev libgeos++-dev libtinyxml2-dev \
            ros-${{ env.ROS_DISTRO }}-ortools-vendor
          echo "/opt/fields2cover-300/lib" | sudo tee /etc/ld.so.conf.d/f2c.conf
          sudo ldconfig
          echo "CMAKE_PREFIX_PATH=/opt/fields2cover-300:$CMAKE_PREFIX_PATH" >> $GITHUB_ENV

      - name: Cache rosdep
        uses: actions/cache@v4
        with:
          path: ~/.ros/rosdep
          key: rosdep-${{ env.ROS_DISTRO }}-${{ hashFiles('ros2/src/**/package.xml', 'tools/motor/package.xml') }}
          restore-keys: |
            rosdep-${{ env.ROS_DISTRO }}-

      # Only opennav_coverage_msgs (the action types) is used from the
      # opennav_coverage submodule; the upstream server subpackages are pinned
      # to F2C 1.2.1 and must NOT be built (they #include fields2cover.h from
      # the v1 install). The Docker image drops the same COLCON_IGNORE markers
      # (Dockerfile stage); mirror that here or opennav_coverage fails the build.
      - name: COLCON_IGNORE upstream opennav_coverage subpackages
        run: |
          for p in opennav_coverage opennav_coverage_bt opennav_coverage_demo \
                   opennav_coverage_navigator opennav_row_coverage; do
            touch "src/opennav_coverage/$p/COLCON_IGNORE" 2>/dev/null || true
          done

      - name: Install rosdep dependencies
        run: |
          source /opt/ros/${{ env.ROS_DISTRO }}/setup.bash
          sudo apt-get update
          rosdep update --rosdistro ${{ env.ROS_DISTRO }}
          # skip-keys:
          # - the opennav_coverage server subpackages are intentionally not
          #   part of this CI build (only opennav_coverage_msgs is used)
          rosdep install \
            --from-paths src \
            --ignore-src \
            --rosdistro ${{ env.ROS_DISTRO }} \
            --skip-keys "opennav_coverage opennav_coverage_bt opennav_coverage_demo opennav_coverage_navigator opennav_row_coverage" \
            -y

      - name: Cache colcon workspace
        uses: actions/cache@v4
        with:
          path: |
            ros2/build
            ros2/install
            ros2/log
          key: colcon-v2-${{ env.ROS_DISTRO }}-${{ hashFiles('ros2/src/**/CMakeLists.txt', 'ros2/src/**/package.xml', 'ros2/src/**/*.cpp', 'ros2/src/**/*.hpp', 'tools/motor/**') }}
          restore-keys: |
            colcon-v2-${{ env.ROS_DISTRO }}-

      - name: Build workspace
        run: |
          source /opt/ros/${{ env.ROS_DISTRO }}/setup.bash
          colcon build \
            --cmake-args -DCMAKE_BUILD_TYPE=Release \
            --parallel-workers $(nproc) \
            --event-handlers console_cohesion+

      - name: Run tests
        run: |
          source /opt/ros/${{ env.ROS_DISTRO }}/setup.bash
          source install/setup.bash
          colcon test \
            --return-code-on-test-failure \
            --event-handlers console_cohesion+
          colcon test-result --verbose

      - name: Validate mowgli_tools package entrypoint
        run: |
          source /opt/ros/${{ env.ROS_DISTRO }}/setup.bash
          source install/setup.bash
          ros2 pkg prefix mowgli_tools
          ros2 run mowgli_tools tune_drive_pid --help | tee /tmp/mowgli_tools_help.txt
          grep -q "Controlled drive PID tuning" /tmp/mowgli_tools_help.txt

      - name: Validate external GNSS launch contract
        working-directory: .
        run: |
          set -eo pipefail
          pwd
          ls -la
          find / -name setup.bash 2>/dev/null | grep install || true
          find / -type d -name install 2>/dev/null | head -50 || true
          set +u
          source /opt/ros/${{ env.ROS_DISTRO }}/setup.bash
          source "${GITHUB_WORKSPACE}/ros2/install/setup.bash"
          set -u
          # This CI job runs on the GitHub-hosted runner checkout under
          # $GITHUB_WORKSPACE, not inside the devcontainer/runtime image where
          # /ros2_ws exists. colcon build runs from the ros2/ subdirectory and
          # writes its overlay to ros2/install.
          echo "=== AMENT PREFIX ==="
          echo "$AMENT_PREFIX_PATH"

          echo "=== COLCON PREFIX ==="
          echo "$COLCON_PREFIX_PATH"

          echo "=== Packages containing GNSS ==="
          ros2 pkg list | sort | grep -Ei 'gnss|gps|universal' || true

          echo "=== Package prefixes ==="
          ros2 pkg prefix mowgli_bringup || true

          echo "=== Launch test ==="
          ros2 launch mowgli_bringup full_system.launch.py --show-args \
            | tee /tmp/full_system_show_args.txt
          ! grep -q "use_universal_gnss" /tmp/full_system_show_args.txt

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: test-results-${{ env.ROS_DISTRO }}
          path: |
            ros2/build/**/test_results/**/*.xml
            ros2/log/latest_test/**
          retention-days: 14

  # ─── Formatting Check ────────────────────────────────────────────────────────
  format-check:
    name: Formatting (clang-format)
    runs-on: ubuntu-24.04
    needs: changes
    if: needs.changes.outputs.ros2 == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install clang-format + git-clang-format
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-format-18
          sudo ln -sf /usr/bin/clang-format-18 /usr/local/bin/clang-format

      # Check only the LINES CHANGED versus the merge-base with main, not the
      # whole tree. The legacy tree is not fully clang-format-clean, so a
      # whole-tree --Werror check is permanently red and useless as a gate;
      # git-clang-format diffs only the touched lines, so this passes when the
      # current change is clean and flags only NEW violations.
      - name: Check formatting (changed lines)
        run: |
          git fetch --no-tags --depth=200 origin main || git fetch --no-tags origin main || true
          BASE=$(git merge-base origin/main HEAD 2>/dev/null || true)
          if [ -z "$BASE" ]; then
            echo "No merge-base with origin/main — skipping."
            exit 0
          fi
          DIFF=$(git-clang-format-18 --binary clang-format-18 \
                   --style=file:ros2/.clang-format --diff "$BASE" -- \
                   ros2/src 2>/dev/null || true)
          case "$DIFF" in
            ""|"no modified files to format"|"clang-format did not modify any files")
              echo "Changed lines are correctly formatted." ;;
            *)
              echo "$DIFF"
              echo "::error::Changed lines are not clang-format clean (run 'make format' / git clang-format)."
              exit 1 ;;
          esac

  # ─── Static Analysis ─────────────────────────────────────────────────────────
  static-analysis:
    name: Static Analysis (cppcheck)
    runs-on: ubuntu-24.04
    needs: changes
    if: needs.changes.outputs.ros2 == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install cppcheck
        run: |
          sudo apt-get update
          sudo apt-get install -y cppcheck

      # Report-only on the CHANGED files versus main. The legacy tree carries
      # cppcheck findings we haven't triaged, so this is informational
      # (continue-on-error) and scoped to what the current change touched —
      # surfaces new findings in the log/artifact without permanently
      # red-gating the workflow. Tighten to a hard gate once the legacy
      # findings are cleared.
      - name: Run cppcheck (changed files, report-only)
        continue-on-error: true
        run: |
          git fetch --no-tags --depth=200 origin main || git fetch --no-tags origin main || true
          BASE=$(git merge-base origin/main HEAD 2>/dev/null || true)
          if [ -n "$BASE" ]; then
            FILES=$(git diff --name-only --diff-filter=ACMR "$BASE" HEAD -- 'ros2/src/' \
              | grep -E '\.(cpp|hpp)$' | grep -v '/test/' | grep -v '/opennav_coverage/' || true)
          else
            FILES=$(find ros2/src/ \( -name "*.cpp" -o -name "*.hpp" \) \
              -not -path "*/test/*" -not -path "*/opennav_coverage/*")
          fi
          if [ -z "$FILES" ]; then
            echo "No changed C++ files — skipping."
            exit 0
          fi
          echo "cppcheck on changed files:"; echo "$FILES"
          # Common cppcheck flags. --inline-suppr lets us silence narrow false
          # positives at the call-site with `// cppcheck-suppress <id>` comments
          # rather than maintaining a global suppress list.
          # Global suppressions for known false positives that aren't worth
          # an inline annotation:
          #   - useInitializationList: ROS2 nodes commonly assign members
          #     (callback groups, QoS, paths read from declare_parameter) in
          #     the body because those calls require the rclcpp::Node
          #     constructor to have run first.
          CPPCHECK_FLAGS=(
            --enable=warning,performance,portability
            --inline-suppr
            --suppress=missingInclude
            --suppress=unmatchedSuppression
            --suppress=useInitializationList
            --suppress=normalCheckLevelMaxBranches
            --error-exitcode=1
          )
          echo "$FILES" | xargs cppcheck "${CPPCHECK_FLAGS[@]}" --xml 2>cppcheck-report.xml
          echo "$FILES" | xargs cppcheck "${CPPCHECK_FLAGS[@]}" --quiet

      - name: Upload cppcheck report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: cppcheck-report
          path: cppcheck-report.xml
          if-no-files-found: ignore
          retention-days: 7
