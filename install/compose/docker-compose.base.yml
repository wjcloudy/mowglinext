# compose/docker-compose.base.yml

# Mowgli ROS2 v3 — main deployment
# Requires Docker Compose v2.20+ (no version key needed)

x-ros2-env: &ros2-env
  ROS_DOMAIN_ID: ${ROS_DOMAIN_ID:-0}
  RMW_IMPLEMENTATION: rmw_cyclonedds_cpp
  CYCLONEDDS_URI: file:///cyclonedds.xml
  ROS_AUTOMATIC_DISCOVERY_RANGE: LOCALHOST
  RCUTILS_COLORIZED_OUTPUT: "1"
  RCUTILS_LOGGING_USE_STDOUT: "1"
  # LIDAR_ENABLED is deliberately NOT passed into the ROS2 container. The
  # stack's LiDAR mode comes from mowgli_robot.yaml's `lidar_enabled` and
  # nothing else; an ambient env var here used to silently override the
  # operator's GUI toggle. LIDAR_ENABLED still lives in .env, where it decides
  # whether the mowgli-lidar CONTAINER is composed in (install/lib/compose.sh).
  ENABLE_FOXGLOVE: ${ENABLE_FOXGLOVE:-true}
  HARDWARE_BACKEND: ${HARDWARE_BACKEND:-mowgli}
  # full_system.launch.py reads these to pick the GNSS adapter; without them
  # the main container always saw the gps/UBX defaults, so unicore/NMEA installs
  # parsed the wrong backend in navsat_to_absolute_pose / gnss status.
  GNSS_BACKEND: ${GNSS_BACKEND:-gps}
  GPS_PROTOCOL: ${GPS_PROTOCOL:-UBX}

services:

  # -------------------------------------------------------------------------
  # Main ROS2 robot service
  # Runs the full mowgli_ros2 stack: hardware bridge, Nav2, behavior trees,
  # coverage planning, SLAM map server, and foxglove_bridge (port 8765).
  # -------------------------------------------------------------------------

  mowgli:
    container_name: mowgli-ros2
    image: ${MOWGLI_ROS2_IMAGE}
    network_mode: host
    ipc: host
    privileged: true
    environment:
      <<: *ros2-env
    command: >
      ros2 launch mowgli_bringup full_system.launch.py
      enable_foxglove:=${ENABLE_FOXGLOVE:-true}
    volumes:
      - /dev:/dev
      - mowgli_maps:/ros2_ws/maps
      # Read-write so calibrate_imu_yaw_node and map_server's
      # set_docking_point can splice dock_pose_x/y/yaw, datum_lat/lon,
      # imu_yaw etc. back into the operator-facing yaml. Other services
      # (ntrip, mavros bridge) keep :ro since they only read config.
      - ./docker/config/mowgli:/ros2_ws/config
      - ./docker/config/cyclonedds.xml:/cyclonedds.xml:ro
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-file: "5"
        max-size: 10m

volumes:
  mowgli_maps:
